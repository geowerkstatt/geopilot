services:
  geopilot:
    environment:
      # Direct upload was removed; Cypress now exercises the cloud-upload (Azurite) flow
      # configured in docker-compose.yml. Ensure the blob container exists on each run.
      CloudStorage__AutoCreateContainer: "true"
