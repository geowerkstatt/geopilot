services:
  geopilot:
    environment:
      # Cypress exercises the cloud-upload (Azurite) flow from docker-compose.yml;
      # auto-create the blob container so it exists on each run.
      Upload__Cloud__AutoCreateContainer: "true"
