processes:
  - id: xtf_validator
    default_config:
      validationProfile: PROFILE-A
pipelines:
  - id: ili_validation
    display_name:
      en: XTF Validation
    steps:
      - id: validation
        display_name:
          en: XTF Validation
        process_id: xtf_validator
        process_config_overwrites:
          validationProfile: PROFILE-B
