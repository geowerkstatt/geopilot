processes:
pipelines:
  - id: ili_validation
    display_name:
      en: XTF Validation
    steps:
      - id: validation
        display_name:
          en: XTF Validation
        process_id: xtf_validator
        process_config_overwrites:
        input:
        output:
          - take: ErrorLog
            as: error_log
            action:
          - take: XtfLog
            as: xtf_log
            action:
              - DOWNLOAD
